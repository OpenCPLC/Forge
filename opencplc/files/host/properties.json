{
  "configurations": [
    {
      "name": "${NAME}",
      "includePath": [
        "${workspaceFolder}/${CORE_DIR}/hal/host/**",
        "${workspaceFolder}/${CORE_DIR}/lib/**",
        "${workspaceFolder}/${PRO_DIR}/**"
      ],
      "defines": [
        "HOST",
        "${DEFINE}",
        "_DEBUG"
      ],
      "cStandard": "gnu17",
      "compilerPath": "gcc"
    }
  ],
  "version": 4
}
