{
  "configurations": [
    {
      "name": "${NAME}",
      "includePath": [
        "${workspaceFolder}/${CORE_DIR}/hal/host/**",
        "${workspaceFolder}/${CORE_DIR}/lib/**",
        "${workspaceFolder}/${PRO_DIR}/**"
      ],
      "defines": [
        "HOST",
        "${DEFINE}",
        "_DEBUG"
      ],
      "compilerPath": "${TOOLS_GCC}",
      "cStandard": "gnu17"
    }
  ],
  "version": 4
}
